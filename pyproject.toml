[project]
name = "tritium-sc"
version = "0.1.0"
description = "Neighborhood-scale Nerf battlefield management system"
requires-python = ">=3.12"
dependencies = [
    "influxdb-client>=1.36.0",
]

[tool.setuptools.packages.find]
where = ["src"]

